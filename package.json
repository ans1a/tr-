{
  "name": "tr-slang",
  "version": "1.0.0",
  "description": "Traduz português para inglês com gírias naturais",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js --external:@vendetta --format=iife"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
